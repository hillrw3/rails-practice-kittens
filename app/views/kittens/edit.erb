<%= image_tag @kitten.image %>


<%= form_for @kitten, url: edit_kitten_path do |f| %>
  <%= f.collection_select :category_id, Category.all, :id, :name, prompt: true %>
  <%= f.submit "Add Category" %>
<% end %>